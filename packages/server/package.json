{
  "name": "uurwerk-server",
  "version": "0.1.0",
  "private": true,
  "description": "Receives published internship days and shows them to a supervisor or a teacher, each behind their own login",
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": { "uurwerk-users": "./bin/uurwerk-users.js" },
  "scripts": {
    "start": "node src/server.js",
    "users": "node bin/uurwerk-users.js"
  }
}
